#! /bin/bash

if xdotool search --classname fzf-dmenu; then
    exit
fi

menu=$(mktemp)
cat >$menu
choice=$(mktemp)
alacritty --class fzf-dmenu -o window.dimensions.columns=56 window.dimensions.lines=15 -e sh -c "fzf <$menu >$choice"
cat $choice 
